force_relearn:
  name: Force Relearn
  description: Reset learned power values and sample count for one or all zones.
  fields:
    zone:
      name: Zone
      description: Climate or switch zone to reset (optional)
      required: false
      selector:
        entity:
          domain: ["climate", "switch"]
